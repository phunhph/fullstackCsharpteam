@model IEnumerable<fullstackCsharp.Models.Payoff>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Payoff1)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PayoffDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdUNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Payoff1)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PayoffDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdUNavigation.IdU)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdPay">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdPay">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdPay">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
