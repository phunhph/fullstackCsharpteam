@model fullstackCsharp.Models.UserRole

@{
    ViewData["Title"] = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Chi tiết</h1>

<div>
    <h4>UserRole</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            Quyền
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.IdRNavigation.RoleName)
        </dd>
        <dt class="col-sm-2">
            Nhân viên
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.IdUNavigation.UserName)
        </dd>
    </dl>
</div>
<div>
    <a class="btn btn-success" asp-action="Edit" asp-route-id="@Model.IdUr">Sửa</a> |
    <a class="btn btn-outline-info" asp-action="Index">Trở lại</a>
</div>
